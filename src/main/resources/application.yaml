server:
  port: 4000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: test123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
